# Multi-threaded-File-Compression-System-TCP
This project is a TCP-based client/server application developed using C# and Windows Forms.  The system allows multiple clients to connect to a server and send files for compression.   The server receives the file, compresses it using GZip compression, then sends the compressed version back to the client.
